version: "3.8"

services:
  coo-assistant:
    build: .
    container_name: coo-assistant
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
