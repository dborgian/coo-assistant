# COO Assistant

AI-powered Chief Operating Officer assistant for internal startup operations. Communicates via Telegram, integrates with Google Calendar, Gmail, and Kanbanchi, and uses Claude for intelligent decision-making.

## Features

- **Telegram Bot** — Bidirectional communication with the founder via grammY
- **Chat Monitoring** — Monitors Telegram groups and DMs via GramJS userbot, classifies urgency, notifies when replies are needed
- **Daily Reports** — Automated morning operations reports generated by Claude
- **Task Management** — Track tasks, set reminders, assign to team members
- **Calendar Sync** — Google Calendar integration (upcoming events, conflict detection)
- **Email Triage** — Gmail integration (important email flagging)
- **Kanbanchi Sync** — Project board monitoring and overdue task alerts
- **AI Brain** — Claude-powered reasoning for message classification, report generation, and query answering

## Tech Stack

- **Runtime:** Node.js 20+ / TypeScript
- **Telegram Bot:** [grammY](https://grammy.dev)
- **Telegram Userbot:** [GramJS](https://github.com/nicedayzhu/gram-js) (for group monitoring)
- **AI:** Anthropic Claude SDK
- **Database:** SQLite via better-sqlite3 + Drizzle ORM
- **Scheduler:** node-cron
- **Validation:** Zod
- **Logging:** Pino

## Architecture

```
Telegram Bot (grammY) + GramJS Userbot
         |
    Service Layer (chat monitor, reports, reminders, calendar, email)
         |
    Claude AI Agent (reasoning & report generation)
         |
    Google APIs (Calendar, Gmail) + Direct APIs (Kanbanchi)
         |
    SQLite Database (persistent state via Drizzle ORM)
```

## Project Structure

```
src/
  index.ts              — Entry point
  config.ts             — Env config with Zod validation
  bot/
    telegram-bot.ts     — Bot setup (grammY)
    commands.ts         — All bot commands
    monitors.ts         — GramJS userbot chat monitoring
  core/
    agent.ts            — Claude AI agent (COO brain)
    scheduler.ts        — Cron jobs (reports, reminders, syncs)
    mcp-client.ts       — MCP server config loader
    google-auth.ts      — Google OAuth2 helper
  models/
    database.ts         — SQLite + Drizzle init
    schema.ts           — All table schemas
  services/
    chat-monitor.ts     — Pending message reminders
    daily-reporter.ts   — Daily ops report
    task-reminder.ts    — Task due reminders
    calendar-sync.ts    — Google Calendar sync
    email-manager.ts    — Gmail integration
    kanbanchi-sync.ts   — Kanbanchi board sync
  utils/
    logger.ts           — Pino logger
    telegram.ts         — Helper to send messages
```

## Quick Start

### 1. Prerequisites

- Node.js 20+
- Telegram Bot Token (from [@BotFather](https://t.me/botfather))
- Telegram API credentials for GramJS (from [my.telegram.org](https://my.telegram.org))
- Anthropic API key
- Google Cloud project with Calendar & Gmail APIs enabled (optional)

### 2. Install

```bash
git clone https://github.com/dborgian/coo-assistant.git
cd coo-assistant
npm install
```

### 3. Configure

```bash
cp .env.example .env
# Edit .env with your credentials
```

### 4. Run

```bash
# Development (with hot reload)
npm run dev

# Production
npm run build
npm start
```

### 5. Talk to your COO

Open Telegram and send `/start` to your bot.

## Bot Commands

| Command | Description |
|---------|-------------|
| `/start` | Initialize the bot |
| `/status` | Operations overview (tasks, messages, team, clients) |
| `/report` | Generate AI-powered operations report |
| `/tasks` | List active tasks (add `overdue` for overdue only) |
| `/remind [person] [task]` | Set a reminder for a team member |
| `/add_employee [name] [email] [role]` | Add team member |
| `/add_client [name] [company] [email]` | Add client |
| `/monitor add/list` | Configure chat monitoring |
| `/help` | Full command reference |
| Any text message | Free-form COO query (answered by Claude) |

## Scripts

| Script | Description |
|--------|-------------|
| `npm run dev` | Start in development mode with hot reload (tsx) |
| `npm run build` | Compile TypeScript to `dist/` |
| `npm start` | Run the compiled app |
| `npm run lint` | Lint with ESLint |
| `npm test` | Run tests with Vitest |

## Implementation Status

- [x] Full TypeScript scaffolding & project structure
- [x] Telegram bot with all commands (grammY)
- [x] Claude AI agent for reasoning & reports
- [x] SQLite database with Drizzle ORM schemas
- [x] Cron scheduler (daily reports, task reminders, chat checks)
- [x] Service layer (chat monitor, daily reporter, task reminders)
- [ ] GramJS userbot integration (real-time group monitoring)
- [ ] Google Calendar sync
- [ ] Gmail email triage
- [ ] Kanbanchi board sync
- [ ] Tests

## License

Private — internal use only.
